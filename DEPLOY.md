# Deploying TaskFlow to Railway

## Environment Variables to set in Railway:
- NODE_ENV = production
- JWT_SECRET = (any long random string, e.g. "taskflow-prod-secret-abc123xyz789")
- PORT = (Railway sets this automatically, do NOT override)
